export default function LabasZuiki({text}) {
  return (
    <h1
      style={{
        color: "pink",
        textAlign: "center",
        textTransform: "uppercase"
      }}
    >
      {text}
    </h1>
  );
}
